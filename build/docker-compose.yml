version: "3.8"

services:
  as-postgres:
    image: postgres:13-alpine
    volumes:
      - $PWD/build/postgres:/var/lib/postgresql/data:rw
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      PGDATA: /var/lib/postgresql/data
      POSTGRES_DB: hospital
